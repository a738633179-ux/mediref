# 贡献指南

感谢你想为医查通贡献内容！本项目最需要的是**准确、简明、可靠的疾病科普条目**。

## 添加一条疾病

1. 复制 `data/diseases.json` 中任意一条记录作为模板
2. 修改以下字段：

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识，小写字母/数字/连字符，如 `common-cold` |
| `name` | 疾病中文名 |
| `aliases` | 常用别名数组，可为空 |
| `category` | 分类，如「呼吸系统」「心血管」「皮肤科」 |
| `overview` | 2–4 句概述 |
| `symptoms` | 常见症状列表（非空） |
| `examinations` | 常用检查方法列表（非空） |
| `treatments` | 治疗/处理原则列表（非空） |
| `prevention` | 预防建议列表（非空） |
| `note` | 免责提示，默认使用统一文案 |

3. 运行校验，确保通过：

```bash
python3 scripts/validate.py
```

4. 提交 Pull Request，并简述你补充的内容与依据。

## 内容要求

- 只写有可靠依据（权威指南、教科书）的通用科普信息
- 不使用“保证治愈”“百分百有效”等绝对化表述
- 涉及药物时说明“遵医嘱”“需医生评估”
- 不针对个人病情给出建议

## 其他贡献

修复页面样式、搜索逻辑、无障碍问题，或改进校验脚本，同样欢迎。

## 代码规范

- 页面逻辑保持零依赖（原生 HTML/CSS/JS）
- 数据变更必须通过 `scripts/validate.py`
